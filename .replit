# https://docs.replit.com — деплой и порты для Telegram-бота с HTTP health check

run = "npm start"

[deployment]
run = ["npm", "run", "start"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 3000
externalPort = 80
